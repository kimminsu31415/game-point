// figma ui 컴포넌트 라이브러리 사용하기

import { Button } from '@/components/ui/button';

export default function Test() {
  return <Button>Click me</Button>;
}

// github ui 컴포넌트 라이브러리 사용하기

import { Button } from '@/components/ui/button';

export default function Test() {
  return <Button>Click me</Button>;
}

// host app 컴포넌트 라이브러리 사용하기

import { Button } from '@/components/ui/button';

export default function Test() {
  return <Button>Click me</Button>;
}